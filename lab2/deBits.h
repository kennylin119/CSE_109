#ifndef DEBITS
#define DEBITS

void print1Byte(uint8_t);    /* accepts a 1 byte value and prints out its bits in memory */
void print4Bytes(uint32_t);  /* accepts a 4 byte value and prints out its bits in memory */
void print8Bytes(uint64_t);  /* accepts a 8 byte value and prints out its bits in memory */

#endif
